# ── LOTO ULTIME — Docker environment ──
# Copy this file to .env before running docker-compose up

# Backend
SECRET_KEY=change-me-to-a-random-string-of-at-least-32-chars
APP_ENV=production

# Frontend port (default 3080)
FRONTEND_PORT=3080

# CORS — set to your actual domain(s) served by Traefik
# CORS_ORIGINS=["https://loto.ton-domaine.fr"]

# Grafana (monitoring profile only)
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
